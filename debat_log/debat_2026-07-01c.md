# Debat Log — 2026-07-01c

**Debator:** Claude
**Scope:** Unit tests for `planResize` (ImageResizeCalculator)

## Changes Reviewed

### `core/util/ImageResizeCalculatorTest.kt` (new)
15 tests covering:
- sampleSize=1 for images at or below maxDimension
- sampleSize halving behavior (both dimensions must exceed 2× max simultaneously)
- Narrow tall image that never triggers halving (sampleSize=1 — correctly documents the algorithm boundary)
- Target width/height for landscape, portrait, square inputs
- Target dimensions unchanged when image is already small
- Aspect ratio preserved within 1% for both orientations
- `require()` guards (zero width, zero height, zero maxDimension)

Two initial test assertions were wrong (assumed incorrect halving steps). Fixed after
tracing the loop — tests now accurately describe the actual `planResize` semantics.

## Findings

### CRITICAL
None.

### WARNING
None.

### MINOR
None.

## Decision

No CRITICALs. Proceed.
