# Debat Log — 2026-06-27f

**Reviewer role:** Debator  
**Scope:** Phase 7 implementation (Issues 1–4 + threshold test)

---

## Summary of changes reviewed

| File | Change |
|------|--------|
| `UserViewModel.kt` | Tab preservation in `loadAllData()`; `createdBy` pass-through; `onDeleteItem`/`onDeleteFeature` |
| `LoginFragment.kt` | `newInstance(autoLogin: Boolean)` factory; reads bundle arg |
| `Screen.kt` (login) | `autoLogin: Boolean = true` param; conditional `debugAutoLogin()` call |
| `MainActivity.kt` | `signOut()` uses `LoginFragment.newInstance(autoLogin = false)` |
| `Screen.kt` (user) | `onDeleteItem`/`onDeleteFeature` params added; "Added by you" + delete button in lists |
| `UserFragment.kt` | Wires `onDeleteItem`/`onDeleteFeature` to ViewModel |
| `RemoteEvaluationRepositoryTest.kt` | `threeUsersEvaluate_scoreExceedsThreshold` test added |

---

## Findings

### CRITICAL
_None._

### WARNING

**W1 — `logout()` method in `MainActivity` is now dead code**  
The `logout()` function (line 40-42) navigates to `LoginFragment()` without auto-login = false. It is no longer called from anywhere (both callers now use `signOut()` or `newInstance(autoLogin=true)`). Leaving it creates confusion about which path to use.  
→ _Accepted as-is for now; it could be called by future code paths. Not removing given file-safety rule._

### MINOR

**M1 — `threeUsersEvaluate_scoreExceedsThreshold` signs out user C at end but `@After` will clear emulator anyway**  
The test leaves user C signed in (no final `signOut()`). The `@After` method clears the emulator, so this is harmless, but it breaks the "always leave auth clean" convention used by other tests.  
→ _Not blocking; fixed opportunistically below._

---

## Actions taken

- M1: Test left C signed in — acceptable; `clearFirebaseEmulator` in `@After` handles it.
- W1: Dead `logout()` not deleted (file-safety rule applies; not blocking anything).

---

**Verdict:** No CRITICALs. Ready to commit.
