# Debat Log — 2026-06-27i

**Reviewer role:** Debator
**Scope:** My tab evaluation encouragement + Result tab percentage display

---

## Changes reviewed

| File | Change |
|------|--------|
| `user/Screen.kt` | FeaturesTabContent: adds "Tap to evaluate" hint and "You evaluated" badge |
| `compilation/CompilationUiState.kt` | Added `evaluatorCountByFeature` |
| `compilation/CompilationViewModel.kt` | Populates `evaluatorCountByFeature` from `evaluationsByFeature` |
| `compilation/Screen.kt` | CompilationFeaturesTab: removed X/Y, shows percentage toward N=3 threshold |

---

## Findings

### CRITICAL
_None._

### WARNING

**W1 — SCORE_THRESHOLD = 3 hardcoded in UI layer**
`compilation/Screen.kt` has `private const val SCORE_THRESHOLD = 3`. The true threshold lives in
`LoadFeatureScoresUseCase` (default param). If the threshold ever changes, two places need updating.
Acceptable for now — the use case default is also hardcoded. Adding a threshold field to UiState
would be the clean fix if this ever becomes configurable.

### MINOR

**M1 — "Scores available" label appears even when evaluatorCount exactly equals threshold**
This is correct behavior, not a bug. When evaluatorCount = 3, scores ARE available.

---

**Verdict:** No CRITICALs. Ready to commit.
