# Debator Review — 2026-06-26d

## Scope

Full README audit against current codebase state. All phases 0–4 are implemented and pushed. This review identifies remaining gaps before closing out the plan.

---

## Audit

### Phase 0 — Firebase Project Setup
Steps 1, 2, 6: Done (user created Firebase project; firestore.rules deployed). ✓

### Phase 1 — Design (Steps 3–7)
All core model, repository interface, scoring engine interface, and UiState files exist and match README spec. ✓

### Phase 2 — Core Logic (Steps 8–11)
EvaluationConverter, ScoreAggregator, ScoreRemapper, DefaultFeatureScoringEngine all implemented and covered by unit tests. ✓

### Phase 3 — Firebase Data Layer (Steps 12–15)
All remote repositories present. EvaluationRecord uses `@ServerTimestamp`. Firestore path is `evaluations/{featureId}/userEvaluations/{userId}`. ✓

### Phase 4 — Use Cases and UI Wiring (Steps 16–20)
- LoadFeatureScoresUseCase ✓
- LoginViewModel: factory, auto-login LaunchedEffect, isAlreadySignedIn, signUp ✓
- UserViewModel: all methods, factory ✓
- CompilationViewModel: all methods, factory ✓
- All Fragments: properly wired ✓
- SRRBaseScreen: `actions` parameter ✓
- BaseFragment: `TopBarActions()` open function ✓
- Both tab Fragments: sign-out IconButton ✓
- MainActivity: `signOut()` ✓

### Phase 5 — Quality (Steps 21–25)

**Steps 21–24 (Unit tests):** 30 unit tests, all passing. ✓

**Step 18 — EvaluationEditor drag-to-reorder: CRITICAL**

README Step 18 explicitly says:
> render a `ReorderableColumn` or `LazyColumn` with `ReorderableItem` from `sh.calvin.reorderable`; each row has a drag handle icon

Current implementation in `user/Screen.kt:282` uses up/down `IconButton` arrows instead of `sh.calvin.reorderable`. The library is declared in `libs.versions.toml` (`reorderable = "2.4.0"`) and added to `build.gradle.kts` (`implementation(libs.reorderable)`) but never used.

**CRITICAL: Must replace arrow-button reorder with sh.calvin.reorderable drag handles.**

**Step 25 (Firebase emulator integration tests): MISSING**

`RemoteEvaluationRepositoryTest.kt` does not exist. README specifies:
- Test: submitEvaluation writes to correct Firestore path
- Test: re-submitting overwrites previous evaluation  
- Test: multiple users all returned by getEvaluationsForFeature
- Uses Firebase emulator (10.0.2.2:8080, 9099)

**CRITICAL: Must write RemoteEvaluationRepositoryTest.kt.**
(Firebase CLI emulator setup requires user action; writing the test file is within Developer scope.)

**Step 26 (Manual E2E):** Manual testing checklist — outside automated scope. Not a code gap.

---

## Summary

| # | Finding | Severity |
|---|---------|----------|
| 1 | EvaluationEditor uses arrow buttons, not sh.calvin.reorderable | CRITICAL |
| 2 | RemoteEvaluationRepositoryTest.kt not written | CRITICAL |
| 3 | Step 26 manual E2E | INFO (manual, not code) |

## Verdict

**BLOCK.** Fix both CRITICALs before committing.
