# Test Log — 2026-06-28b

**Tester**  
**Scope:** Item image feature — push gate (instrumented tests on physical device)

---

## Instrumented Tests

**Device:** SM-S926N (Galaxy S24+) — `R3CX50BXZVD`  
**Command:** `./gradlew connectedDebugAndroidTest` (with `ANDROID_SERIAL=R3CX50BXZVD`)  
**ADB reverse:** tcp:8080, tcp:9099 confirmed before run  
**Result:** `BUILD SUCCESSFUL` — **60/60 tests passed** (0 skipped, 0 failed)  
**Duration:** 2 min 14 s

**Notes:**
- First run (before `clean`) had 1 spurious failure: `itemsTab_allItemNamesAreDisplayed` — "No compose hierarchies found" — diagnosed as stale compiled class from incremental build after image-feature changes. No code defect.
- `./gradlew clean` resolved it. All 60 passed on the clean run.
- `UserScreenUiTest.kt` needed 1 line added (`onAddItemImageUrlChange = {}`) to match the updated `UserScreen` signature — committed as `71d75a3`.

---

## Post-test actions

- APK reinstalled via `./gradlew installDebug`
- ADB reverse ports tcp:8080 and tcp:9099 restored and verified
- Pushed to remote: commits `b3089cb` + `71d75a3` → `main`

---

## Verdict: PASS — push gate approved
