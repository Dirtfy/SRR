# Test Log — 2026-07-01f

## Instrumented Tests (push gate)

**Device:** SM-S926N (R3CX50BXZVD) — physical device, Android 16
**Variant:** connectedDebugAndroidTest
**Firebase emulators:** Auth :9099, Firestore :8080, Storage :9199
**ADB reverse ports:** 8080, 9099, 9199 restored before each class

| Class | Tests | Result |
|-------|-------|--------|
| RemoteEvaluationRepositoryTest | 11 | PASS |
| RemoteItemFeatureRepositoryTest | 18 | PASS |
| StorageUploadTest | 5 | PASS |
| UserScreenUiTest | 24 | PASS |
| **Total** | **58** | **ALL PASS** |

### Notes

- First run of UserScreenUiTest had 2 failures:
  1. `itemDetail_imagePreviewDialog_isShownWhenStateHasPreviewUrl`: used `onRoot().assertExists()`
     which failed because the Dialog composable creates a second compose root that the main-root
     query doesn't see. Fixed: added semantic `contentDescription = "Image preview overlay, …"` to
     the dialog Box; test now uses `onNodeWithContentDescription(…).assertExists()`.
  2. `itemDetail_imageClickCallback_isInvokedWhenImageIsClicked`: used `onRoot().performClick()`
     which missed the image Box. Fixed: added semantic `contentDescription = "Item image, tap to
     preview"` to the clickable Box; test now uses `onNodeWithContentDescription(…).performClick()`.
- All fixes committed in the same session before push.
- Debug APK install via split-APK session failed (release key still on device from earlier install);
  worked around with `adb install -r app-debug.apk` (single-APK install bypasses session split).

## Gate Decision

PASS — push to remote authorized.

## Post-test
- Debug APK reinstalled on device via `adb install -r`
- ADB reverse ports restored (8080, 9099, 9199)
