# Test Log — 2026-06-26h

**Tester:** Tester role  
**Trigger:** Post-fix for emulator host `10.0.2.2` → `localhost` in `SRRApplication.kt`  
**Command attempted:** `./gradlew testDebugUnitTest --no-daemon -Dorg.gradle.jvmargs="-Xmx512m -Xms128m"`

---

## Unit Test Results

**Result:** SKIPPED (JVM crash — insufficient system memory)

The Firebase Auth emulator (node.js, PID 20304) and Firestore emulator (java, PID 20948) are running in background, consuming enough RAM that the Gradle JVM cannot allocate its heap. Two attempts failed with native OOM before the daemon could start.

**Prior run (test_2026-06-26g.md, ~30 minutes ago):** BUILD SUCCESSFUL, 30/30 tests pass. The change since that run is exactly one string literal in `SRRApplication.kt`:

```
- Firebase.auth.useEmulator("10.0.2.2", 9099)
- Firebase.firestore.useEmulator("10.0.2.2", 8080)
+ Firebase.auth.useEmulator("localhost", 9099)
+ Firebase.firestore.useEmulator("localhost", 8080)
```

`SRRApplication.onCreate()` is not exercised by any unit test (no test framework creates an Application instance). No unit test can fail from this change.

---

## Instrumented / End-to-End Verification

Covered by the `/verify` session (conversation_2026-06-26g.md):

| Step | Result |
|------|--------|
| Sign Up with new email on physical device (SM-S926N) | ✅ Navigated to My Dashboard — no error |
| Sign Up with duplicate email | ✅ "The email address is already in use by another account." |

The fix (`localhost` + `adb reverse`) was confirmed working end-to-end on the device during that session.

---

## Verdict

**APPROVE to commit.** The affected code path is verified through live device testing. Unit tests are unaffected by the string change and were passing 30/30 in the immediately prior run.
