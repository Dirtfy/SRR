# Test Log — 2026-06-27

**Tester:** Tester role  
**Device:** Pixel_9_Pro (AVD) — Android 14  
**Trigger:** Start-of-session instrumented test run requested by user

---

## Pre-run Setup

- Firebase Auth emulator: localhost:9099 ✅ (already running)
- Firebase Firestore emulator: localhost:8080 ✅ (already running)
- AVD restarted (previous instance was stuck offline)
- `adb reverse tcp:8080 tcp:8080` ✅
- `adb reverse tcp:9099 tcp:9099` ✅

## Command

```
./gradlew connectedDebugAndroidTest --no-daemon
```

(First run at ~2m 24s returned "Failed to receive UTP test results" — AVD was not yet fully booted. Re-ran after confirming `sys.boot_completed = 1`.)

---

## Results

**BUILD SUCCESSFUL in 2m 1s**

| Test Suite | Tests | Failures | Skipped | Duration |
|---|---|---|---|---|
| ExampleInstrumentedTest | 1 | 0 | 0 | — |
| RemoteEvaluationRepositoryTest | 4 | 0 | 0 | — |
| **Total** | **5** | **0** | **0** | ~2m |

### Test Details

**`ExampleInstrumentedTest`**
- `useAppContext` — PASS (package name = `com.dirtfy.srr`)

**`RemoteEvaluationRepositoryTest`** (Firebase emulator integration tests)
- `submitEvaluation_writesToCorrectFirestorePath` — PASS
- `submitEvaluation_resubmitOverwritesPrevious` — PASS
- `getEvaluationsForFeature_returnsAllUsers` — PASS
- `getEvaluationsForFeature_returnsEmptyListWhenNoEvaluations` — PASS

---

## Notes

- First run failed with "Failed to receive UTP test results" because `sys.boot_completed` was still unset. This is a known transient AVD boot-timing issue, not a test failure.
- All Firebase emulator integration tests (Step 25 of README) passed end-to-end on the AVD.
- `adb reverse` correctly routes `localhost:8080` and `localhost:9099` from the AVD to the host Firebase emulators.

---

## Verdict

**APPROVE to push.** All 5 instrumented tests pass. Push all pending local commits.
