# Test Log — 2026-06-27c

**Role:** Tester  
**Scope:** Instrumented tests — push gate for commit f056158 (Phase 6)  
**Command:** `$env:ANDROID_SERIAL = "R3CX50BXZVD"; ./gradlew connectedDebugAndroidTest`  
**Device:** SM-S926N (physical, Android 16) — serial R3CX50BXZVD  
**Result:** BUILD SUCCESSFUL (27s)

## Test Results

| Test | Result |
|------|--------|
| `submitEvaluation_writesToCorrectFirestorePath` | PASSED |
| `submitEvaluation_resubmitOverwritesPrevious` | PASSED |
| `getEvaluationsForFeature_returnsAllUsers` | PASSED |
| `getEvaluationsForFeature_returnsEmptyListWhenNoEvaluations` | PASSED |
| `ExampleInstrumentedTest` | PASSED |

5/5 tests passed.

## Incident: First Run Failed (Multi-Device Collision)

First run (without `ANDROID_SERIAL`) ran on both SM-S926N and Pixel_9_Pro(AVD) simultaneously. Both devices hit the same Firebase Local Emulator and collided on `createUserWithEmailAndPassword` — same email addresses used by each device's parallel test run. Root cause: the teardown `DELETE localhost:99099` on the AVD cannot reach the host Firebase emulator (AVD `localhost` ≠ host). Dirty emulator state from physical device accumulated on re-runs.

**Fix applied:**
- Added `ANDROID_SERIAL` guard to CLAUDE.md rule 5
- Added warning comment to `RemoteEvaluationRepositoryTest.kt` header
- Manually cleared emulator data (`DELETE /emulator/v1/projects/.../accounts` and `.../documents`) before re-run

## Verdict

**PUSH APPROVED.** All instrumented tests pass on physical device.
