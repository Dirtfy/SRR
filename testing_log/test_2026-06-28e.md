# Test Log — 2026-06-28e

**Tester**  
**Scope:** Storage emulator fix + image upload tests — push gate

---

## Unit Tests

**Result:** `BUILD SUCCESSFUL` — all pass (UP-TO-DATE from prior run)

---

## Instrumented Tests

**Device:** SM-S926N (Galaxy S24+) — `R3CX50BXZVD`  
**Command:** `./gradlew connectedDebugAndroidTest` (ANDROID_SERIAL=R3CX50BXZVD)  
**ADB reverse:** tcp:8080, tcp:9099, tcp:9199  
**Firebase emulators:** Auth:9099, Firestore:8080, Storage:9199 (all running)  
**Result:** `BUILD SUCCESSFUL` — **69/69 tests passed** (0 skipped, 0 failed)  
**Duration:** 1 min 7 s

### New tests passing (9 new since last run)
- `UserScreenUiTest`: 5 image picker UI state tests
- `StorageUploadTest`: 4 Storage emulator integration tests
  - `uploadItemImage_successReturnsHttpUrl`
  - `uploadItemImage_urlContainsItemsPath`
  - `uploadItemImage_unauthenticated_fails`
  - `uploadTwice_producesTwoDistinctUrls`

### Fix applied mid-session
`= runBlocking { ... }` inherits the lambda's last expression return type. Methods annotated `@Before`/`@Test` must return `void` (JUnit 4 rule). Fixed by using block body for `setUp`/`tearDown` and placing `file.delete()` before the final `assertTrue` in each test so the block ends with a `Unit`-returning expression.

---

## Post-test actions

- APK reinstalled via `./gradlew installDebug`
- ADB reverse ports tcp:8080, tcp:9099, tcp:9199 restored and verified

---

## Verdict: PASS — push gate approved
