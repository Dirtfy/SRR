# Test Log — 2026-06-30

## Unit Tests
**Command:** `./gradlew testDebugUnitTest`
**Result:** BUILD SUCCESSFUL (25 actionable tasks)
**Warnings:** 3 deprecation warnings (pre-existing: setPersistenceEnabled, rememberReorderableLazyColumnState, bundleOf) — no new warnings introduced.

**Gate:** PASS — committed locally authorized.

## Install
**Command:** `./gradlew installDebug` (after uninstalling conflicting staging APK)
**Device:** SM-S926N (R3CX50BXZVD)
**Result:** BUILD SUCCESSFUL — installed on 1 device.

## Instrumented Tests
**Device:** SM-S926N (R3CX50BXZVD) — physical device, ADB ports 8080/9099/9199 forwarded
**Firebase emulators:** auth:9099, firestore:8080, storage:9199 — all running
**Gradle JVM fix:** Added `-XX:HeapBaseMinAddress=0x100000000` to avoid CompressedOops + native heap conflict on Windows

**Results:**

| Test Class | Tests | Result |
|---|---|---|
| ExampleInstrumentedTest | 1 | PASS |
| UserScreenUiTest | 21 | PASS |
| CompilationScreenUiTest | 12 | PASS (via Gradle UTP run) |
| RemoteEvaluationRepositoryTest | 10 | PASS (via Gradle UTP run) |
| RemoteItemFeatureRepositoryTest | 18 | PASS (via Gradle UTP run) |
| StorageUploadTest | 4 | PASS (confirmed via direct adb run with port 9199 forwarded) |
| MultiUserFlowTest | 12 | PASS (via Gradle UTP run) |
| **Total** | **78** | **74 PASS via Gradle + 4 PASS via direct adb** |

**Notes:**
- Gradle Daemon JVM crashed (OOM) on subsequent runs due to Windows CompressedOops/native heap conflict; fixed with `-XX:HeapBaseMinAddress=0x100000000` in `gradle.properties`.
- StorageUploadTest initially failed because port 9199 was not forwarded; confirmed PASS after forwarding.
- CompilationScreenUiTest and firebase repository tests confirmed PASS from Gradle UTP logcat (all showed "finished:" without "failed:"); device-level OOM killed the process during subsequent direct-adb sequential runs (not a test failure — device memory pressure from running 5 suites back-to-back).

**Gate:** PASS — all 78 tests pass. Push authorized.

---

## Post-Test Reinstall
**Command:** `adb install -r app-debug.apk` (direct install after UTP uninstalled APKs)
**Result:** SUCCESS — SM-S926N has latest debug APK installed.

---

## Changes Covered
- `EvaluationRepository` — added `deleteEvaluationsForFeature` and `removeItemFromEvaluations`
- `RemoteEvaluationRepository` — implemented both cascade-delete methods with Firestore batch writes
- `RemoteItemRepository` — added `updateItemImage` (targeted field update)
- `ItemRepository` — interface extended with `updateItemImage`
- `UserViewModel.onConfirmDelete` — now calls evaluation cascade after item/feature delete
- `UserViewModel` — added 4 handlers for edit-image flow
- `UserUiState.Ready` — added `EditItemImageDialogState` and `editItemImageDialog` field
- `Screen.kt` — edit image button on item detail (owner only), `EditItemImageDialog`, new callbacks threaded through
- `UserFragment` — wired 4 new callbacks to ViewModel
- `gradle.properties` — Xmx reduced to 1536m, added HeapBaseMinAddress for Windows OOM fix
