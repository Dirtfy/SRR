# Test Log — 2026-06-27e

**Role:** Tester  
**Scope:** Instrumented tests — push gate for evaluation UX fix + emulator auto-clear task  
**Command:** `$env:ANDROID_SERIAL = "R3CX50BXZVD"; ./gradlew connectedDebugAndroidTest`  
**Device:** SM-S926N (physical, Android 16)  
**Result:** BUILD SUCCESSFUL (1m 15s)

## Test Results

5/5 passed on SM-S926N.

## Incident: Second run also failed with account collision

Before the `clearFirebaseEmulator` Gradle task was added, a second consecutive run failed with the same `FirebaseAuthUserCollisionException` as before. Root cause confirmed: the device-side HTTP DELETE in `@After` teardown doesn't reliably reach the host emulator if ADB reverse is not active at teardown time.

**Fix applied:** Added `clearFirebaseEmulator` Gradle task to `app/build.gradle.kts` that calls DELETE on both emulator endpoints from the HOST machine before `connectedDebugAndroidTest` runs. This guarantees clean emulator state regardless of ADB reverse status.

## Verdict

**PUSH APPROVED.** All instrumented tests pass.
