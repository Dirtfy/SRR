# Test Log — 2026-06-30d

## Unit Tests
**Command:** `./gradlew testDebugUnitTest`
**Result:** BUILD SUCCESSFUL (25 actionable tasks: 5 executed, 20 up-to-date)

**New tests added:**
- `StorageCleanupLogicTest.findImageUrl_returnsUrlWhenItemExists` — PASS
- `StorageCleanupLogicTest.findImageUrl_returnsNullWhenItemHasNoImage` — PASS
- `StorageCleanupLogicTest.findImageUrl_returnsNullWhenItemNotFound` — PASS
- `StorageCleanupLogicTest.findImageUrl_returnsNullOnEmptyList` — PASS
- `StorageCleanupLogicTest.findImageUrl_returnsCorrectItemWhenMultiplePresent` — PASS

**Gate:** PASS — committed locally authorized.

## Changes Covered
- `StorageRepository` — added `deleteImage(url: String): Result<Unit>`
- `RemoteStorageRepository` — implemented `deleteImage` via `getReferenceFromUrl().delete()`
- `storage.rules` — split `write` → `create, update` (size guard) + `delete` (auth only); deployed to production
- `UserViewModel.onConfirmDelete` — deletes Storage image after successful item Firestore delete
- `UserViewModel.onSubmitEditItemImage` — deletes old Storage image when replacing with new one
- `UserViewModel.onDismissEditItemImageDialog` — deletes uploaded-but-unsaved image on dialog cancel

## Instrumented Tests
Pending — run per-class after Firebase emulators started.
