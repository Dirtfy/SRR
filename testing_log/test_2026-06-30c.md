# Test Log — 2026-06-30c

## Unit Tests

**Command:** `./gradlew testDebugUnitTest`
**Result:** BUILD SUCCESSFUL
**Verdict:** PASS — commit gate cleared

---

## Instrumented Tests

**Device:** SM-S926N (physical, serial R3CX50BXZVD)
**Emulators:** Auth:9099, Firestore:8080, Storage:9199 (all UP)
**ADB reverse:** tcp:8080, tcp:9099, tcp:9199

### Context: Samsung Backup interference

Running all 78 instrumented tests in a single session (~90 s) caused Samsung Backup to kill the `com.dirtfy.srr` process at the ~37-test mark, producing "Instrumentation run failed due to Process crashed." Each test class was run separately (≤ 50 s per class) to avoid the Samsung Backup window.

### Results by class

| Class | Tests | Result |
|-------|-------|--------|
| `RemoteEvaluationRepositoryTest` | 11 | PASS |
| `RemoteItemFeatureRepositoryTest` | 18 | PASS |
| `StorageUploadTest` | 4 | PASS |
| `UserScreenUiTest` | 21 | PASS |
| **Total** | **54** | **ALL PASS** |

### Key fixes diagnosed in this session

1. `deleteEvaluationsForFeature` security rule required feature doc to exist; changed ViewModel to delete evaluations **before** the feature doc.
2. Added `exists()` guard to Firestore security rule so `get(feature).data.createdBy` is never called on a null doc.
3. Updated cascade-delete tests to create feature documents before running the cascade.
4. Added `android:allowBackup="false"` to manifest to reduce Samsung Backup interference.
5. Storage emulator (port 9199) must be started alongside auth+firestore for `StorageUploadTest`.

### Verdict

APPROVED for push.
